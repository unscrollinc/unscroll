DELETE FROM tablename
WHERE id IN (SELECT id
              FROM (SELECT id,
                             ROW_NUMBER() OVER (partition BY title ORDER BY id) AS rnum
                     FROM tablename) t
              WHERE t.rnum > 1);

